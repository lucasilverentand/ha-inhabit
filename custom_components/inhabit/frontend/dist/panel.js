// Placeholder - This file is generated by the frontend build process
// Run `cd frontend && npm install && npm run build` to generate the actual panel

console.warn('Inhabit: Panel not built. Run `cd frontend && npm install && npm run build`');

class HaFloorplanBuilder extends HTMLElement {
  set hass(hass) {
    if (!this._initialized) {
      this._initialized = true;
      this.innerHTML = `
        <div style="padding: 24px; text-align: center;">
          <h2>Inhabit Floor Plan Builder</h2>
          <p>Frontend not built. Please run:</p>
          <code style="display: block; background: #f5f5f5; padding: 12px; margin: 12px; border-radius: 4px;">
            cd frontend && npm install && npm run build
          </code>
        </div>
      `;
    }
  }
}

customElements.define('ha-floorplan-builder', HaFloorplanBuilder);
